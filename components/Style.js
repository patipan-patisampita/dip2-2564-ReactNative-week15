import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        backgroundColor: 'yellow',
        padding: 20,
        paddingTop: 30,
    },

    textTitle: {
        color: 'blue',
        fontSize: 20,
        fontWeight: 'bold',
        marginTop: 15,
        marginBottom: 15,
    }
})